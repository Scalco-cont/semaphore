---
- name: Executar script Python no Windows
  hosts: windows
  become: yes
  become_user: TI  # Eleva para o usuário Administrador
  tasks:
    - name: Executar script Python
      win_shell: python C:\Windows\Temp\script.py
