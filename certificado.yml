---
- name: List certificates in the store
  ansible.windows.win_certificate_store:
    path: Cert:\CurrentUser\My
    state: list
  register: certs

- name: Remove all certificates
  ansible.windows.win_certificate_store:
    path: "{{ item.PSPath }}"
    state: absent
  loop: "{{ certs.certificates }}"
